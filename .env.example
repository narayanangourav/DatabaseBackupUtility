# Optional Gradio UI defaults
DBBACKUP_UI_HOST=127.0.0.1
DBBACKUP_UI_PORT=7860

# Set credentials in your shell or secret manager; never commit real values.
DBBACKUP_PASSWORD=
DBBACKUP_SLACK_WEBHOOK=
